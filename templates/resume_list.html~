<!doctype html>
<html lang="en">
  <head>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="{{url_for('static', filename='css/jquery-ui.css')}}">
    <link rel="stylesheet" href="{{url_for('static', filename='css/bootstrap.css')}">

    <title id="title"></title>
  </head>
  <body>
    <div id="TOP_DIV" class="container-fluid w-100 h5 text-white"></div>
    <div class="container-fluid h-100 w-75 h3">
      <!-- -->
      <table class="table table-striped table-bordered table-hover ">
        <thead>
          <tr>
            <th>Unique ID</th>
            <th>NameID</th>
            <th>Location</th>
            <th>Date Modified</th>
          </tr>
        </thead>
        <tbody id="resume_list_table">
        </tbody>
      </table>


    </div>
    </div>
    <div id="END" class="container row w-100"></div>

    <!-- JavaScript -->
    <!-- jQuery first, then Popper.js, then Bootstrap JS -->
    <script src="{{url_for('static', filename='js/jquery-3.3.1.js')}}"></script>
    <script src="{{url_for('static', filename='js/popper.js')}}"></script>
    <script src="{{url_for('static', filename='js/bootstrap.js')}}"></script>
    <script src="{{url_for('static', filename='js/jquery-ui.js')}}"></script>
    <script src="{{url_for('static', filename='js/resume.js')}}"></script>
    <script>$("#sortable").sortable();</script>
  </body>
</html>
